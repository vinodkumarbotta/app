pipeline {
    agent any 
    stages {
        stage('Back-end') {
            steps {
                sh 'python -V'
            }
        }
        stage('Front-end') {
            steps {
                sh 'node --version'
            }
        }
    }
}